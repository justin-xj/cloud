server:
  port: 10000

spring:
  application:
    name: nacos-study
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}
    sentinel:
      transport:
        dashboard: 127.0.0.1:8080

