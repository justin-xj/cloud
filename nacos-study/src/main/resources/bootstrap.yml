server:
  port: 10001

spring:
  application:
    name: nacos-study
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

      discovery:
        server-addr: 127.0.0.1:8848
test:
  justin: 341
management:
  endpoints:
    web:
      exposure:
        include: '*'
  #让服务下线,封锁线程
  endpoint:
    shutdown:
      enabled: true
